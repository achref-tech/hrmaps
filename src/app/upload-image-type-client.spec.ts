import { UploadImageTypeClient } from './upload-image-type-client';

describe('UploadImageTypeClient', () => {
  it('should create an instance', () => {
    expect(new UploadImageTypeClient()).toBeTruthy();
  });
});
