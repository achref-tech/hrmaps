import { DemandeClientType } from './demande-client-type';

describe('DemandeClientType', () => {
  it('should create an instance', () => {
    expect(new DemandeClientType()).toBeTruthy();
  });
});
